<?php

(isset($_GET['instructions']) && (!empty($_GET['instructions']))) ? $instructions = $_GET['instructions'] : $instructions = "Looking for information related to John Smith from the Reconnaisance Battalion between the dates March and April 1941.";


